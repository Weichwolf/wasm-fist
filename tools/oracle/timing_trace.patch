--- a/src/hardware/vga_memory.cpp
+++ b/src/hardware/vga_memory.cpp
@@ -535,0 +536,24 @@
+	if (getenv("FIST_DACREQTRACE") && ph == 0x2d916)
+		fprintf(stderr, "FIST_DACREQ %.6f cs=%04x eip=%08x val=%02x IF=%u\n",
+			PIC_FullIndex(), SegValue(cs), reg_eip, val, GETFLAG(IF) ? 1u : 0u);
+	if (getenv("FIST_DACREQTRACE") && ph >= 0x2d774 && ph < 0x2d778)
+		fprintf(stderr, "FIST_DACVEC %.6f cs=%04x eip=%08x ph=%08x val=%02x\n",
+			PIC_FullIndex(), SegValue(cs), reg_eip, ph, val);
+	if (getenv("FIST_DACREQTRACE") && (ph == 0x46c || ph == 0x2d5e2))
+		fprintf(stderr, "FIST_TICK %.6f cs=%04x eip=%08x ph=%08x val=%02x\n",
+			PIC_FullIndex(), SegValue(cs), reg_eip, ph, val);
+	if (getenv("FIST_DACREQTRACE") && SegValue(cs) == 0x4ec3 &&
+		reg_eip >= 0xbe2 && reg_eip <= 0xbff && PIC_FullIndex() < 600.0)
+		fprintf(stderr, "FIST_DACISR %.6f eip=%08x ph=%08x val=%02x\n",
+			PIC_FullIndex(), reg_eip, ph, val);
+	if (getenv("FIST_MGAENTRYLOG") && reg_eip >= 0x4a3 && reg_eip <= 0x4cc) {
+		static Bit32u last_eip = 0xffffffffu;
+		if (reg_eip != last_eip) {
+			fprintf(stderr, "FIST_MGA_ENTRY %.6f cs=%04x eip=%04x ph=%08x val=%02x\n",
+				PIC_FullIndex(), SegValue(cs), reg_eip, ph, val);
+			last_eip = reg_eip;
+		}
+	}
+	if (getenv("FIST_E584LOG") && reg_eip >= 0xe584 && reg_eip <= 0xe6db)
+		fprintf(stderr, "FIST_E584 %.6f cs=%04x eip=%04x ds=%04x dsb=%08x ph=%08x val=%02x\n",
+			PIC_FullIndex(), SegValue(cs), reg_eip, SegValue(ds), SegPhys(ds), ph, val);
@@ -820,0 +845,3 @@
+	if (getenv("FIST_INTRO_PIXELLOG") && off == 18704)
+		fprintf(stderr, "FIST_PIXEL %.6f cs=%04x eip=%08x flatip=%08x value=%02x\n",
+			PIC_FullIndex(), SegValue(cs), reg_eip, SegPhys(cs) + reg_eip, val & 0xff);
--- a/src/dos/dos_files.cpp
+++ b/src/dos/dos_files.cpp
@@ -32,0 +33 @@
+#include "pic.h"
@@ -508,0 +510,2 @@
+	if (getenv("FIST_KDV_FILELOG") && strstr(name, ".KDV"))
+		fprintf(stderr, "FIST_KDV_FILE %.6f name=%s flags=%u\n", PIC_FullIndex(), name, flags);
--- a/src/hardware/vga_dac.cpp
+++ b/src/hardware/vga_dac.cpp
@@ -22,0 +23,4 @@
+#include "pic.h"
+#include "regs.h"
+#include <stdlib.h>
+#include <stdio.h>
@@ -95,0 +100,4 @@
+	if (getenv("FIST_DACTRACE") && val == 0 && PIC_FullIndex() < 800.0)
+		fprintf(stderr, "FIST_DAC %.6f cs=%04x eip=%08x entry17=%u,%u,%u\n",
+			PIC_FullIndex(), SegValue(cs), reg_eip,
+			vga.dac.rgb[17].red, vga.dac.rgb[17].green, vga.dac.rgb[17].blue);
@@ -117,0 +126,4 @@
+		if (getenv("FIST_DACTRACE") && vga.dac.write_index == 17 && PIC_FullIndex() < 800.0)
+			fprintf(stderr, "FIST_DAC17 %.6f cs=%04x eip=%08x value=%u,%u,%u\n",
+				PIC_FullIndex(), SegValue(cs), reg_eip,
+				vga.dac.rgb[17].red, vga.dac.rgb[17].green, vga.dac.rgb[17].blue);
--- a/src/ints/int10.cpp
+++ b/src/ints/int10.cpp
@@ -27,0 +28,3 @@
+#include "pic.h"
+#include <stdio.h>
+#include <stdlib.h>
@@ -53,0 +57 @@
+		if (getenv("FIST_INT10TRACE")) fprintf(stderr, "FIST_INT10_ENTER %.6f mode=%02x cs=%04x eip=%08x\n", PIC_FullIndex(), reg_al, SegValue(cs), reg_eip);
@@ -54,0 +59 @@
+		if (getenv("FIST_INT10TRACE")) fprintf(stderr, "FIST_INT10_EXIT %.6f mode=%02x\n", PIC_FullIndex(), reg_al);
--- a/src/hardware/timer.cpp
+++ b/src/hardware/timer.cpp
@@ -28,0 +29,3 @@
+#include "regs.h"
+#include <stdio.h>
+#include <stdlib.h>
@@ -192,0 +196,4 @@
+	if (getenv("FIST_PITTRACE") && port == 0x40 && PIC_FullIndex() < 600.0)
+		fprintf(stderr, "FIST_PIT40 %.6f cs=%04x eip=%08x val=%02x mode=%u state=%u cntr=%u new=%u\n",
+			PIC_FullIndex(), SegValue(cs), reg_eip, val, pit[0].mode,
+			pit[0].write_state, pit[0].cntr, pit[0].new_mode ? 1u : 0u);
@@ -291,0 +299,3 @@
+	if (getenv("FIST_PITTRACE") && ((val >> 6) & 3) == 0 && PIC_FullIndex() < 600.0)
+		fprintf(stderr, "FIST_PIT43 %.6f cs=%04x eip=%08x val=%02x\n",
+			PIC_FullIndex(), SegValue(cs), reg_eip, val);
--- a/src/hardware/pic.cpp
+++ b/src/hardware/pic.cpp
@@ -29,0 +30,3 @@
+#include "regs.h"
+#include <stdio.h>
+#include <stdlib.h>
@@ -145,0 +149,3 @@
+	if (getenv("FIST_PICTRACE") && port == 0x21 && PIC_FullIndex() < 600.0)
+		fprintf(stderr, "FIST_PICMASK %.6f cs=%04x eip=%08x val=%02x\n",
+			PIC_FullIndex(), SegValue(cs), reg_eip, val);
